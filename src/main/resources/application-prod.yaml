server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST}:3306/${DATABASE_NAME}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
